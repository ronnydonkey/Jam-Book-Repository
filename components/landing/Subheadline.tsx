import React from 'react';

const Subheadline: React.FC = () => {
  return (
    <p className="text-lg text-gray-600 text-center mt-4 max-w-xl mx-auto">
      Search, transpose, and print jam-ready chord sheets for every song in your set.
    </p>
  );
};

export default Subheadline;
